# Exclude large directories from the Docker build context.
# Without this, Docker sends node_modules (~300 MB) and
# .next (~100 MB) to the daemon on every build — very slow.

node_modules
.next
.env
.env.local
.env*.local
*.log
npm-debug.log*
.DS_Store
.git
.gitignore
README.md
CHANGELOG.md

package com.edham.logistics.feature.accountant.domain.usecase

import com.edham.logistics.core.utils.Result
import com.edham.logistics.feature.accountant.domain.model.Debt
import com.edham.logistics.feature.accountant.domain.repository.AccountantRepository
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject

class GetDebtsUseCase @Inject constructor(
    private val accountantRepository: AccountantRepository
) {
    
    suspend operator fun invoke(
        page: Int = 1,
        pageSize: Int = 20,
        status: String? = null,
        customerId: String? = null,
        isOverdue: Boolean? = null
    ): Flow<Result<List<Debt>>> {
        return accountantRepository.getDebts(page, pageSize, status, customerId, isOverdue)
    }
}

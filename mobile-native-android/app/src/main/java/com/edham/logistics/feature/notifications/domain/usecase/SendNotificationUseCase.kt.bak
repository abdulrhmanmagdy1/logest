package com.edham.logistics.feature.notifications.domain.usecase

import com.edham.logistics.core.utils.Result
import com.edham.logistics.feature.notifications.domain.model.SendNotificationRequest
import com.edham.logistics.feature.notifications.domain.repository.NotificationRepository
import javax.inject.Inject

class SendNotificationUseCase @Inject constructor(
    private val notificationRepository: NotificationRepository
) {
    
    suspend operator fun invoke(request: SendNotificationRequest): Result<String> {
        return notificationRepository.sendNotification(request)
    }
}

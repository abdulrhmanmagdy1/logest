<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_timeline_padding"
    tools:showIn="@layout/fragment_shipment_detail_production">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/shipment_progress"
        style="@style/TextAppearance.Edham.TitleMedium"
        android:layout_marginBottom="@dimen/spacing_lg" />

    <!-- Timeline Container -->
    <LinearLayout
        android:id="@+id/timelineContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Timeline items will be added programmatically -->

    </LinearLayout>

    <!-- Current Status -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="@dimen/spacing_xl"
        android:padding="@dimen/spacing_md"
        android:background="@drawable/shape_status_container">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_info"
            app:tint="@color/status_info"
            android:layout_marginEnd="@dimen/spacing_md" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/currentStatusText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="الشحنة قيد النقل"
                style="@style/TextAppearance.Edham.LabelMedium" />

            <TextView
                android:id="@+id/currentStatusTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="آخر تحديث: 2 ساعة مضت"
                style="@style/TextAppearance.Edham.BodySmall"
                android:layout_marginTop="@dimen/spacing_xs" />
        </LinearLayout>

    </LinearLayout>

</LinearLayout>
